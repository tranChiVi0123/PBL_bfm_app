const config = {
  // API_URL : `https://www.example.com`,
  API_URL: process.env.API_URL || `http://localhost:5000/api/`,
  // API_URL: process.env.API_URL || `https://www.example.com`,
};

export default config;